/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'firebasestorage.googleapis.com',
        'images.pexels.com',
        'cdn.pixabay.com'
        ],
    },
  };
  
  export default nextConfig;
  